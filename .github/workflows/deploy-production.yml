name: Deploy Production

on:
  push:
    branches: [production]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    env:
      DEPLOY_HOST: 64.227.149.74
      DEPLOY_USER: shrenik
      DEPLOY_PATH: /var/www/g2a-activities-frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Write .env.production
        run: |
          cat <<EOF > .env.production
          VITE_API_BASE_URL=${{ secrets.PROD_VITE_API_BASE_URL }}
          VITE_BASE_URL=${{ secrets.PROD_VITE_BASE_URL }}
          VITE_RAZORPAY_KEY_ID=${{ secrets.PROD_VITE_RAZORPAY_KEY_ID }}
          EOF

      - name: Build (main app + bike-rentals widget)
        run: npm run build:all:production

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Add server to known_hosts
        run: ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Ensure remote deploy path exists
        run: ssh "$DEPLOY_USER@$DEPLOY_HOST" "mkdir -p '$DEPLOY_PATH'"

      - name: Deploy to production
        run: |
          rsync -avz --delete \
            -e "ssh" \
            dist/ "$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH/"
